# Інструмент для Кореляційного Аналізу (Correlation Analyzer)

Цей проєкт є настільним додатком на Python для проведення кореляційного аналізу. Додаток дозволяє користувачам завантажувати CSV-файли, автоматично очищувати дані та аналізувати зв'язки між змінними за допомогою коефіцієнта Пірсона.

Ключовою особливістю є не лише візуалізація (теплова карта), але й **інтерактивна текстова інтерпретація** коефіцієнтів, що робить аналіз доступним для користувачів без глибоких знань у статистиці.

## 🚀 Основні можливості

* **Сучасний GUI:** Інтерфейс побудований на `Tkinter` з використанням тем `ttkbootstrap` (темна тема).
* **Автоматичне очищення:** Програма автоматично видаляє нечислові колонки та рядки з пропущеними значеннями (`NaN`).
* **Теплова карта (Heatmap):** Візуалізація всієї матриці кореляцій за допомогою `Seaborn` та `Matplotlib`.
* **Фільтрація:** Окрема вкладка "Групування" показує лише ті зв'язки, які перевищують заданий поріг (наприклад, `r > 0.8`).
* **Інтерактивна Інтерпретація:** Унікальна вкладка, яка пояснює будь-який коефіцієнт простою мовою, коли користувач натискає на таблицю.

## 🛠️ Встановлення

Для запуску проєкту вам знадобиться Python 3 та наступні бібліотеки.

1.  Клонуйте репозиторій:
    ```bash
    git clone https://github.com/[Ваш_Нікнейм]/[Назва_Вашого_Проекту].git
    ```

2.  Встановіть необхідні залежності:
    ```bash
    pip install pandas numpy seaborn matplotlib ttkbootstrap
    ```

## 🏃‍♂️ Як користуватися

Проєкт складається з двох основних скриптів:

**1. Генерація даних (Опціонально):**
Якщо у вас немає власного CSV-файлу, ви можете згенерувати "реалістичний" набір даних, запустивши:

```bash
python generate_data_file.py
```
```bash
python Python.py
```
